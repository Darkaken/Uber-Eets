

<!DOCTYPE html>
<html>
<title>Uber Eets</title>
<meta charset="UTF-8">
<meta name="viewport" content="width=device-width, initial-scale=1">
<link rel="stylesheet" href="https://www.w3schools.com/w3css/4/w3.css">
<style>
body {font-family: "Times New Roman", Georgia, Serif; background-color: LightGray}
h1, h2, h3, h4, h5, h6 {
  font-family: "Playfair Display";
  letter-spacing: 5px;
}
</style>
<body>

<!-- Navbar (sit on top) -->
<div class="w3-top">
  <div class="w3-bar w3-white w3-padding w3-card" style="letter-spacing:4px;">
    <a href="/" class="w3-bar-item w3-button">Uber Eets</a>
    <!-- Right-sided navbar links. Hide them on small screens -->
    <div class="w3-right w3-hide-small">
      <a href="/" class="w3-bar-item w3-button">Salir</a>
    </div>
  </div>
</div>

<body>
  <div class="w3-content" style="max-width:1100px">
    <div class="w3-row w3-padding-64" id="about">
      <div class="w3-col m6 w3-padding-large">

        <h3>Solicitudes de Restaurantes:</h3>

        <form action="admin" method="post">
          <input type="hidden" name="authenticity_token" value="<%= form_authenticity_token %>">
          <ol>
            <% @restaurantes.each do |rest| %>
            <% id = rest.id %>
            <li> <%= "Nombre: #{rest.nombre} " %>
              <a href= <%="/info_rest/#{id}"%>> Perfil </a>
              <p>Estado corriente: <%= get_estado(rest.estado) %></p>
              <p>
                <label> Estado Solicitud: </label>
                <input type="radio" name="datos[<%= rest.id  %>]" value=0> Aceptado
                <input type="radio" name="datos[<%= rest.id  %>]" value=1> Pendiente
                <input type="radio" name="datos[<%= rest.id  %>]" value=2> Rechazado
                <input type="radio" name="datos[<%= rest.id  %>]" value=3> Borrar
              </p>
            </li>
            <% end %>
          </ol>
          <input type="submit" value="Guardar Cambios">
        </form>

        <hr>

        <h3>Estadisticas de Restaurantes</h3>

        <ul>
          <p>Cantidad de restaurantes aprobados: <%=@count_rest%></p>
        </ul>

      </div>
    </div>
  </div>
</body>



<!-- Footer -->
<footer class="w3-center w3-light-grey w3-padding-32">
  <p>Uber Eets</p>
</footer>

</body>
</html>
