<!DOCTYPE html>
<html lang="en" dir="ltr">
  <head>
    <title>Uber Eets: Perfil Restaurante</title>
  </head>
  <body>
    <h2> Perfil de <%= @restaurante.nombre %></h2>

    <% if @success %>
      <h4><%= "Cambios Guardados!" %></h4>
    <% end %>

    <% if @restaurante.avatar.attached? == true %>
      <%= image_tag(@restaurante.avatar, size: "200x200") %>
    <% end %>

    <%= form_for @restaurante, url: "/restaurantprofile/#{@restaurante.id}/cambiar_img" do |form| %>
      <p>Cambiar Imagen:</p>
      <%= form.file_field :avatar %> <br>
      <%= form.submit "Guardar Imagen" %>
    <% end %>

    <p>Nombre: <%= @restaurante.nombre %></p>
    <p>Correo: <%= @restaurante.correo %></p>
    <p>Direccion: <%= @restaurante.direccion %></p>
    <p>Telefono: <%= @restaurante.telefono %></p>

    <form action=<%="/restaurantprofile/#{@restaurante.id}"%> method="post">
      <input type="hidden" name="authenticity_token" value="<%= form_authenticity_token %>">
      <p>
        <label>Cambiar nombre:</label>
        <input type="text" name="datos[nombre]">
      </p>
      <p>
        <label>Cambiar correo:</label>
        <input type="text" name="datos[correo]">
      </p>
      <p>
        <label>Cambiar direccion: </label>
        <input type="text" name="datos[direccion]">
      </p>
      <p>
        <label>Cambiar telefono:</label>
        <input type="text" name="datos[telefono]">
      </p>
      <p>
        <label>Cambiar contraseña:</label>
        <input type="password" name="datos[password]">
      </p>
      <input type="submit" value="Guardar Cambios">
    </form>

    <a href=<%="/restaurantdashboard/#{@restaurante.id}"%>> Volver a Dashboard</a>

  </body>
</html>
